name: CI

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore "CloudScale Learning.slnx"

      - name: Build
        run: dotnet build "CloudScale Learning.slnx" --no-restore --configuration Release

      - name: Run tests
        run: dotnet test "CloudScale Learning.slnx" --no-build --configuration Release
